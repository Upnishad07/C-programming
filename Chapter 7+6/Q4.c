/* Q- Write a program that:
1. Declares an integer array with values: 5, 10, 15, 20, 25
2. Create a pointer ptr pointing to the first element
3. Print all elements by:
   a. Incrementing the pointer (ptr++)
   b. Using pointer arithmetic (ptr+i)
   c. Calculate the size of the array using pointers
4. Find the largest element using pointer arithmetic
    */

#include <stdio.h>
int main()
{
    
    return 0;
}